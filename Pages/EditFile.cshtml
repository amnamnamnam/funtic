@page "/editfile"
 @model EditFileModel
 @{
 }
 <form enctype="multipart/form-data" method="post">
     <table class="table">
        <tr>

        </tr>
          <tr>
              <td>Folder</td>
                <a download href="@Url.Page("EditFile", "DownloadFileFromFolder", new { recipeId = Model.recipeId})">Загрузить этикетку</a>
         </tr> 
     </table> 
 </form>